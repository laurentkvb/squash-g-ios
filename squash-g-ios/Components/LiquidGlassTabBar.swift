// LiquidGlassTabBar removed.
// The app now uses the native `TabView` with system materials for the bottom tab bar.
// Kept this stub to avoid accidental build breakages in previews; remove file entirely if you prefer.

import SwiftUI

struct LiquidGlassTabBar_Placeholder: View {
    var body: some View {
        EmptyView()
    }
}

#Preview {
    LiquidGlassTabBar_Placeholder()
}
